export class Item {
    constructor({ id = undefined, name = '', description = '' } = {}) {
        this.id = id
        this.name = name
        this.description = description
    }
}

// CUSTOMIZA: Esta entidad es un placeholder. Adapta los campos a tu dominio real
// (por ejemplo, Product, Course, Post). Renombra el archivo y referencias si cambias el nombre.

